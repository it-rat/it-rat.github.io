{
  "_why": [
    "Invariant 3 of CLAUDE.md says a number on this site is a claim with an owner, and until",
    "2026-08-04 it carried `(not enforced)` beside it. Checking every figure took a venv, one",
    "git clone and reading two type signatures, and FOUR OF SEVEN WERE WRONG: trailryx said",
    "1,031 tests where the repository ran 1,064, tokenfuse said 513 where it runs 709, engram",
    "said 424 where it collects 466, verdryx said 217 where it collects 292. None was wrong the",
    "day it was written. That is the whole problem: a figure on a marketing page has no owner",
    "and no clock, and the person it misleads is a stranger who cannot check it.",
    "",
    "This file gives every one of them both. `scripts/service-numbers.sh` refuses a publish whose",
    "pages disagree with what is written here, and refuses a page that states a figure nothing",
    "here owns.",
    "",
    "It CANNOT check that a recorded value is still true: this repository has no access to the",
    "others' test suites, and a network call in a publish gate trades silent staleness for a",
    "flaky deploy. What it makes impossible is the silent case. What keeps the values true is",
    "somebody running the commands below and writing the date back, and `measured_on` is what",
    "says how long since anybody did."
  ],
  "entries": [
    {
      "page": "services/trailryx.html",
      "claim": "1,172 tests \u00b7 31 crates",
      "occurrences": 1,
      "repo": "TAIPANBOX/trailryx",
      "command": "cargo test --workspace, summed; and ./scripts/readme-numbers.sh, which gates the same figure inside that repository",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "The strongest entry here: the repository refuses its own push when this number drifts, so the site only has to keep up with a figure that is already true."
    },
    {
      "page": "services/tokenfuse.html",
      "claim": "804 tests",
      "occurrences": 1,
      "repo": "TAIPANBOX/tokenfuse",
      "command": "cargo test --all, summed over every `test result` line",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "Found stale by this file's own first run: the page said 513 and `cargo test --all` sums to 709. The gap, not the number, is why this file exists, so it is recorded here rather than only in git history. Unlike trailryx, this repository has no gate on its own README figure, so nothing but this entry will notice the next drift."
    },
    {
      "page": "services/idryx.html",
      "claim": "25 detectors",
      "occurrences": 5,
      "repo": "TAIPANBOX/idryx",
      "command": "grep -rhA1 'func (.*) Name() string' internal/detect/detectors/*.go | grep -oE 'return \"[a-z_]+\"' | sort -u | wc -l",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "Correct, and now counted rather than merely agreed with. Every detector implements Name(); there are 22 distinct ones, from agent_shadow_tool to unmanaged_egress. The earlier entry here said no command could reproduce this, which was true of the greps tried at the time and not of the type system: the interface is the enumeration."
    },
    {
      "page": "services/engram.html",
      "claim": "510 tests",
      "occurrences": 1,
      "repo": "Engram",
      "command": "python3 -m pytest --collect-only -q, in a venv with jsonschema, numpy, rfc8785, sqlite-vec and pytest-asyncio",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "Was 424 on the page and collects 466. One file, tests/test_encryption.py, is SKIPPED for want of sqlcipher3, which has no wheel for Python 3.14, so the true figure on a machine that has it is higher than this. Recorded as the number this command reproduces, with the condition named, rather than as an estimate of the number it would reach elsewhere."
    },
    {
      "page": "services/verdryx.html",
      "claim": "300 tests",
      "occurrences": 2,
      "repo": "verdryx",
      "command": "python3 -m pytest --collect-only -q",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "Was 217 on the page and collects 292, with no errors and nothing skipped."
    },
    {
      "page": "services/sphere.html",
      "claim": "588 tests",
      "occurrences": 3,
      "repo": "sphere-ios",
      "command": "grep -rhcE '^\\s*@Test' --include='*.swift' . | awk '{s+=$1} END {print s+0}'",
      "status": "measured",
      "measured_on": "2026-08-05",
      "note": "Correct. The repository was cloned to check it: 588 swift-testing @Test macros, which is also what its own README badge says and what its CI gates. The one figure on this site that was already right and had simply never been verified from here."
    },
    {
      "page": "services/platform.html",
      "claim": "32 entries",
      "occurrences": 3,
      "repo": "TAIPANBOX/agent-stack-go",
      "command": "grep -n 'MaxDepth = ' chain/chain.go",
      "status": "measured",
      "measured_on": "2026-08-05",
      "note": "Not a test count at all, which is why nothing could verify it while it sat in a list of test counts. It is a specification constant: the delegation chain in an Agent Passport is capped at 32 entries, `const MaxDepth = 32` in agent-stack-go/chain/chain.go:19, and SPEC.md 5.1 says the same. Recorded here so the next reader does not go looking for a suite."
    },
    {
      "page": "services/platform.html",
      "claim": "82 documented and already fixed",
      "occurrences": 1,
      "repo": "TAIPANBOX/stack-k8s",
      "command": "./scripts/gotchas-classified.sh, which prints the total and the four-way split",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "Said 70 while the repository counted 79, and the prose beside it said sixty-nine, now seventy-eight. This check walked past both because its pattern knew tests, crates, detectors and entries and the page used a fifth noun: a stale figure sitting inside the thing written to catch stale figures. The pattern is wider now, which is the fix; that it needed widening at all is the lesson."
    },
    {
      "page": "services/heraldyx.html",
      "claim": "v0.2.2",
      "occurrences": 1,
      "status": "measured",
      "repo": "TAIPANBOX/heraldyx",
      "command": "gh api repos/TAIPANBOX/heraldyx/tags --jq '.[0].name'; heraldyx publishes a tag and a GHCR image and no GitHub release, so the tag is the shipped thing and `gh release view` would wrongly report nothing; the page states it once, in `shipped v0.2.2, pinned in the deploy`, where the version IS the claim: it says what the stack-k8s manifests pin, so `latest` would make it false",
      "measured_on": "2026-08-05"
    },
    {
      "page": "services/platform.html",
      "claim": "v0.6.0",
      "occurrences": 1,
      "status": "measured",
      "repo": "TAIPANBOX/agent-stack-go",
      "command": "gh release view -R TAIPANBOX/agent-stack-go --json tagName --jq .tagName; this page states it four times, twice inside install commands, so a tag cut in that repository makes four claims wrong here at once; down to one occurrence, the `go get` line, which stays pinned because agent-stack-go's own README forbids @latest for the library. `go install` of the agent-conform COMMAND is now @latest, since nothing lands in a consumer's go.mod",
      "measured_on": "2026-08-10"
    },
    {
      "page": "services/scopyx.html",
      "claim": "v0.1.2",
      "occurrences": 4,
      "repo": "TAIPANBOX/scopyx",
      "command": "gh api repos/TAIPANBOX/scopyx/tags --jq '.[0].name'; scopyx publishes a tag and TWO multi-arch GHCR images, the service and a `-chromium` variant, and the page states the version four times: the spec rail, the header of the measured run, and both docker run lines. The last two are instructions somebody follows, so all four must agree",
      "status": "measured",
      "measured_on": "2026-08-10",
      "note": "Bumped from v0.1.0 on 2026-08-10 and the measured panel was RE-RUN rather than relabelled: all four requests were sent again to the published v0.1.2 image against a fixture policy, and every verdict matched the row it is drawn beside, including the header refusal, which comes back as a JSON-RPC -32602 naming the argument. Relabelling a measured panel with a new version without re-running it is precisely the fault this file exists to prevent."
    }
  ]
}
